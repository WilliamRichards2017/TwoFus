#ifndef __SRC_VARIANT_HPP__
#define __SRC_VARIANT_HPP__

#include <string>
#include <vector>

#include "api/BamMultiReader.h"
#include "api/BamWriter.h"

class variant{

public:
  variant();
  variant(const variant &);
  variant(const BamTools::BamAlignment &);
  
private:
  
BamTools:BamAlignment al_;
  

};

#endif
